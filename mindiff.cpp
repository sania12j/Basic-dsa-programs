#include <bits/stdc++.h>
using namespace std;
int find min difference(int arr[], int n, int m);
{
    if (m == 0 || n = 0)
}
return0;
sort(arr, arr + n);
if (n < m)
    return -1;
int min_difference = INT_MAX;
for (int i = 0; i + m - 1 < n; i++)
{
    if (difference < min_difference)
        ;
    min_difference = difference;
}
return min_difference;
}
int main()
{
    int arr = { 1,
                2,
                3,
                3,
                4,
                4 } int m = 2;
    int n = 4;
    cout << arr();
    return 0;
}
